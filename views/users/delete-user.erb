<h1>Who would you like to delete?</h1>

<form action="/confirm-delete-user">
  <%User.all.each do |b|  %>
      <label for="<%= b.id %>"><%= b.name %></label>
      <input id= "<%= b.id %>" type="radio"  name="user_id" value="<%=b.id%>"><br>
      <br>

  <%end%>
  <input type="submit">
</form>

<a href="/home">GO HOME!</a>